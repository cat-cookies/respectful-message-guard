# 系統架構

```mermaid
flowchart TD
    A[開啟 GitHub Pages 靜態網站] --> B[法制介紹頁]
    B --> C{確認已閱讀}
    C -- 未確認 --> B
    C -- 確認 --> D{勾選本分頁不再顯示}
    D -- 是 --> E[sessionStorage 僅記錄說明頁狀態]
    D -- 否 --> F[不寫入任何儲存空間]
    E --> G[訊息檢核頁]
    F --> G

    G --> H[貼上原始訊息]
    H --> I[本機規則引擎]
    I --> I1[侮辱、威脅、嘲諷與權勢語氣]
    I --> I2[性或性別、持續聯絡與跟蹤風險]
    I --> I3[對案家、服務對象或民眾不禮貌]
    I --> I4[貼圖、表情符號與過度標點]
    I --> I5[身分證、電話、電子郵件、地址、病歷號]
    I --> J[產生較安全、可複製版本]
    J --> K[逐項列出不宜原因及法制提醒]
    K --> L[複製或清除]

    I -. 不使用 .-> M[後端 API]
    I -. 不使用 .-> N[雲端模型]
    I -. 不使用 .-> O[資料庫與分析追蹤]
```

## 威脅模型與控制

| 風險 | 控制 |
|---|---|
| 訊息內容傳到第三方 | 無後端、無 API，CSP `connect-src 'none'` |
| 瀏覽器持久保存 | 不使用 localStorage、IndexedDB、Cookie；只用 sessionStorage 保存說明頁顯示狀態 |
| 第三方程式庫供應鏈 | 不使用 CDN、字型服務或第三方 JavaScript |
| 網頁追蹤 | 無分析工具、像素、廣告或遠端圖片 |
| 複製後外洩 | 明確提醒共用電腦、剪貼簿同步與雲端剪貼簿風險 |
| 規則誤判為法律結論 | 統一標示為「風險提示」，不輸出違法或成立霸凌的結論 |
| 法規過時 | 首頁與頁尾顯示法規查核日期，官方來源採外部連結 |
