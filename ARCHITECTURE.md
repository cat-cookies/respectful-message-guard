# 系統架構

```mermaid
flowchart TD
    A[開啟 GitHub Pages 靜態網站] --> B[第一頁：中華民國法制分流介紹]
    B --> C{使用者確認已閱讀}
    C -- 否 --> B
    C -- 是 --> D{本分頁關閉前不再顯示}
    D -- 勾選 --> E[sessionStorage 僅記錄說明頁狀態]
    D -- 未勾選 --> F[不寫入任何持久儲存]
    E --> G[訊息檢核頁]
    F --> G

    G --> H[貼上原始訊息]
    H --> I[Unicode 正規化與零寬字元清理]
    I --> J[本機第一輪規則引擎]
    J --> J1[辱罵、人格貶抑、羞辱式反問]
    J --> J2[性別貶抑、性意味、性別歧視]
    J --> J3[威脅、解僱/排班/考績施壓、申訴報復]
    J --> J4[排擠冷落、資訊阻斷、不合理工作分派]
    J --> J5[持續聯絡、守候、追蹤暗示]
    J --> J6[案家與服務對象不禮貌]
    J --> J7[貼圖、表情符號、過度標點]
    J --> J8[身分證、電話、電子郵件、地址、病歷號]

    J --> K[移除高風險句／中性化可保留內容／遮罩個資]
    K --> L[可供正式紀錄的語氣整理]
    L --> M{第二輪防漏掃描}
    M -- 仍含明顯辱罵/威嚇 --> N[捨棄殘留內容，改用中性安全模板]
    M -- 未發現殘留 --> O[保留處理後內容]
    N --> P[產生可直接複製版本]
    O --> P
    P --> Q[逐項列出原片段、風險理由與法制提醒]
    Q --> R[複製或清除]

    J -. 不使用 .-> S[後端 API]
    J -. 不使用 .-> T[雲端模型]
    J -. 不使用 .-> U[資料庫、分析追蹤與廣告 SDK]
```

## 設計原則

第一輪規則負責「辨識與解釋」，第二輪防漏負責「不得把明顯高風險原句重新輸出」。即使第一輪改寫規則有遺漏，只要建議版本仍含明顯辱罵、性別羞辱、威嚇或典型嘲弄，第二輪就捨棄殘留內容並改用中性模板。

風險分數不是違法機率，也不是霸凌成立機率。分數只用於排序傳送前的溝通風險；真正法律判斷仍須看權勢關係、工作必要性、頻率、重大情節、主觀意願、客觀影響與完整證據。

## 威脅模型與控制

| 風險 | 控制 |
|---|---|
| 訊息內容傳到第三方 | 無後端、無 API，CSP `connect-src 'none'` |
| 瀏覽器持久保存 | 不使用 localStorage、IndexedDB、Cookie；只用 sessionStorage 保存說明頁顯示狀態 |
| 第三方程式庫供應鏈 | 不使用 CDN、字型服務或第三方 JavaScript |
| 網頁追蹤 | 無分析工具、像素、廣告或遠端圖片 |
| 規則被空格、全形或零寬字元繞過 | 使用 NFKC 正規化、零寬字元清理及常見拆字變體規則 |
| 第一輪漏掉高風險字句 | 第二輪殘留防漏掃描；命中即改用安全模板 |
| 複製後外洩 | 明確提醒共用電腦、剪貼簿同步與雲端剪貼簿風險 |
| 規則誤判為法律結論 | 統一標示為「風險提示」，不輸出違法或成立霸凌的結論 |
| 法規過時 | 首頁與頁尾顯示法規查核日期，官方來源採外部連結 |
